const fields=[...document.querySelectorAll('#order-form input,#order-form textarea')];
const money=n=>`$${(Number(n)||0).toFixed(2)}`;
const prettyDate=v=>v?new Date(`${v}T00:00:00`).toLocaleDateString('en-AU',{day:'numeric',month:'short',year:'numeric'}):'—';
function defaults(){const n=new Date();orderDate.value=n.toISOString().slice(0,10);orderNo.value=`M3D-${n.toISOString().slice(2,10).replaceAll('-','')}-${String(n.getHours()).padStart(2,'0')}${String(n.getMinutes()).padStart(2,'0')}`;quantity.value=1}
function update(){fields.forEach(f=>document.querySelectorAll(`[data-out="${f.id}"]`).forEach(o=>o.textContent=(f.type==='date'?prettyDate(f.value):f.value.trim())||'—'));const q=Math.max(1,Number(quantity.value)||1),u=Number(unitPrice.value)||0,d=Number(deposit.value)||0,t=q*u;document.querySelector('[data-money="unitPrice"]').textContent=money(u);document.querySelector('[data-money="total"]').textContent=money(t);document.querySelector('[data-money="deposit"]').textContent=money(d);document.querySelector('[data-money="balance"]').textContent=money(Math.max(0,t-d))}
fields.forEach(f=>f.addEventListener('input',update));document.querySelector('#save-pdf').onclick=()=>{update();window.print()};document.querySelector('#clear-order').onclick=()=>{if(confirm('Clear all order details?')){document.querySelector('#order-form').reset();defaults();update()}};defaults();update();
