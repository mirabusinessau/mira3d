const toggle=document.querySelector('.mobile-toggle');const nav=document.querySelector('.main-nav');if(toggle)toggle.addEventListener('click',()=>nav.classList.toggle('open'));let cart=Number(localStorage.getItem('mira3dCart')||0);const count=document.querySelector('.cart-count');const show=()=>{if(count)count.textContent=cart};show();document.querySelectorAll('.add-cart').forEach(b=>b.addEventListener('click',()=>{cart++;localStorage.setItem('mira3dCart',cart);show();b.textContent='Added ✓';setTimeout(()=>b.textContent='Add to cart',900)}));